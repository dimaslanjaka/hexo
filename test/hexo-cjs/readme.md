Run the CJS test for the local `hexo` package

1. From this folder, install the local package as a dependency:

```bash
npm install ../..
```

2. Run the test:

```bash
npm start
```

This will run `node index.cjs` which requires `hexo`.
