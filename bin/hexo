#!/usr/bin/env node

'use strict';

if (typeof require == 'undefined') {
  import('hexo-cli').then((lib) => {
    (lib.default || lib)();
  });
} else {
  const lib = require('hexo-cli');
  (lib.default || lib)();
}
